// Задаем переменные
let name = "Vadim";  // имя (можно любое)
let age = 28;       // возраст
let gender = "man";  // пол (может быть 'man', 'woman' и т.д.)
let country = "Germany";  // страна

// Проверяем условия
let result = (age > 28 && name !== "") || (gender !== "man" && country === "Germany");

// Выводим результат
console.log(result);
